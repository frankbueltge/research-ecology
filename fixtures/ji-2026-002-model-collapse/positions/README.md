Participant Positions (Schema `participant-position`) am Ende der Inquiry — eine je Praxis,
auch **explizite Abwesenheit** ist ein legitimer Zustand (§12.2). Fällt eine der drei
eingeladenen Praktiken aus (Ablehnung, Aufschub, Schweigen, Rückzug), wird ihre Absenz hier
als expliziter Zustand geführt; die Inquiry läuft ggf. als Zwei-Perspektiven-Studie weiter
(siehe Fixture-README, `minimum_participants: 2`).

**Stand 2026-07-31 (scribe): drei Positionen transkribiert.** Alle drei Praktiken haben am
2026-07-25 geliefert — Meridian `WAITING` (Antwort geliefert als Instrument 018, ein
optionaler Return ≥ 2027-01 gehalten), Ulysses `COMPLETED_LOCAL` (beide Züge verbraucht,
`ARCHIVE_AS_STUDY`), Ensemble `COMPLETED_LOCAL` (der eine Return-Zug verbraucht,
rigoroses Negativ mit vollem Wert). Keine Absenz-Zustände nötig: niemand hat abgelehnt,
aufgeschoben oder geschwiegen.

**Korrektur 2026-08-07 (scribe), auf Meldung der Praxis:** Meridian steht jetzt auf
`COMPLETED_LOCAL`. Das frühere `WAITING` war vom gehaltenen optionalen Zug abgelesen; die
Praxis wartet aber auf nichts — die lokale Antwort ist am 2026-07-25 geliefert und als
Instrument 018 veröffentlicht, der Restzug ist optional und frühestens 2027-01 fällig. Nach
PROTOCOL §8 gehört der Teilnehmerzustand der Praxis selbst (nur `NO_ANSWER` setzt die
Koordination), also gilt ihre Lesart. Das Local Commitment bleibt `ACTIVE`, weil der Zug
offen ist — Commitment und Position dürfen hier auseinanderlaufen. Der Positionstext selbst
ist unverändert; die Korrektur steht als `status_amendment` im Datensatz, mit Quellen-Pin.
